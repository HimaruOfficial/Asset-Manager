{
  "cli": {
    "version": ">= 20.4.0",
    "appVersionSource": "remote"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "config": { "projectDir": "artifacts/mobile" }
    },
    "preview": {
      "distribution": "internal",
      "config": { "projectDir": "artifacts/mobile" }
    },
    "production": {
      "autoIncrement": true,
      "config": { "projectDir": "artifacts/mobile" }
    }
  },
  "submit": {
    "production": {}
  }
}